{
  "name": "FaturaCartao",
  "type": "object",
  "properties": {
    "conta_cartao_id": {
      "type": "string",
      "title": "Cart\u00e3o"
    },
    "mes_referencia": {
      "type": "string",
      "title": "M\u00eas Refer\u00eancia"
    },
    "data_vencimento": {
      "type": "string",
      "format": "date",
      "title": "Data Vencimento"
    },
    "valor_total": {
      "type": "number",
      "title": "Valor Total"
    },
    "status": {
      "type": "string",
      "enum": [
        "aberta",
        "paga_total",
        "vencida"
      ],
      "title": "Status"
    },
    "data_pagamento": {
      "type": "string",
      "format": "date",
      "title": "Data Pagamento"
    },
    "valor_pago": {
      "type": "number",
      "title": "Valor Pago",
      "default": 0
    },
    "lancamento_bancario_id": {
      "type": "string",
      "title": "ID do LancamentoBancario que pagou a fatura (preenchido em pagamento 1:1; em multi-d\u00e9bito a verdade fica em VinculoExtrato)"
    }
  },
  "required": [
    "conta_cartao_id",
    "mes_referencia",
    "data_vencimento",
    "valor_total",
    "status"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}